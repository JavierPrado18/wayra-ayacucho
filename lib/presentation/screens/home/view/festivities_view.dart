import 'package:flutter/widgets.dart';

class FestivitiesView extends StatelessWidget {
  const FestivitiesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("Fetivities"),);
  }
}